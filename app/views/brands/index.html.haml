.wrapper_with_padding
  %h2 Here are the LeftySeth Brands 
  %table.table.table-responsive.table-hover
    %thead
      %tr
        %th Company
        %th Website
        %th View
        %th{:colspan => "3"}
    %tbody
      - @brands.each do |brand|
        %tr
          %td= brand.company   
          %td= brand.website   
          %td= link_to 'View', brand_path(brand.id)
